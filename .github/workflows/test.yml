name: Run tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:


jobs:
  Bot:
    runs-on: ubuntu-latest
    environment: env
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup
        run: cd docker && docker build -t sp2137 . && cd ..

      - name: Run tests
        run: docker run --rm -v $(pwd):/project sp2137 ./venv/bin/python3 ./project/bot.py -DGIT_PASSWORD=${{secrets.GIT_PASSWORD}} -DLOG_FILE=project/logs/test.log -DFILE_LOG_LEVEL=DEBUG -DLOCAL_DRIVE_STORE=./project/data -DCACHE_DIR=./project/cache test

      - name: Archive
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: logs
          path: |
            **/*.log
